# デプロイ手順書 — notion-sprint-worker

PMO Slack Bot (Cloudflare Worker + Slack + Notion + OpenAI) のデプロイ手順。

## 基本情報

| 項目 | 値 |
|---|---|
| 本番 Worker 名 | `notion-sprint-worker-dev` |
| 本番 URL | https://notion-sprint-worker-dev.tomoya-kotetsu.workers.dev |
| Cloudflare アカウント | tomoya.kotetsu@aice.co.jp |
| account_id | `43ac0fa6a0cf47be7839e6178a2588d0` |
| Slack App 名 | ALOHA_CAT |
| 本番 KV | `NOTIFY_CACHE_DEV` (id: `6e1d5ae41cec40189b2f653e8dcee0b3`) |
| git (本番) | https://github.com/Tomoya148665/notion_pmo.git (upstream) |

> **重要**: 本番 Worker 名は `notion-sprint-worker-dev`（末尾 `-dev`）です。
> 過去に `notion-sprint-worker`（無印）という別 Worker が存在し、そちらにデプロイしても
> 本番に反映されない事故がありました。無印は削除済み。`wrangler.toml` の `name` と
> `account_id` で固定しているので、通常は意識不要です。

## デプロイ手順

```bash
# 1. Cloudflare 認証がtomoya.kotetsuか確認
npx wrangler whoami
#   → "tomoya.kotetsu@aice.co.jp" でなければ:
#   npx wrangler login

# 2. 型チェック（エラーがあれば修正してから進む）
npx tsc --noEmit

# 3. デプロイ（wrangler.toml に account_id 固定済みなので env 指定不要）
npx wrangler deploy

# 4. デプロイ確認
npx wrangler deployments status
```

`account_id` を `wrangler.toml` に固定しているため、別アカウントでログイン中の場合は
deploy が **エラーで止まり**、誤アカウントへのデプロイを防ぎます。

## 動作確認

```bash
# ライブログを表示（別ターミナルで開いたまま Slack でテスト）
npx wrangler tail notion-sprint-worker-dev --format pretty
```

Slack で `@ALOHA_CAT テストタスクを<名前>に追加 SP3` 等を投げ、
ログに以下が出れば正常:
- `Intent hint: create — using light fetch path`（高速パス）
- `Assignee ... → notionUserId=...`（担当者解決成功）

## Secret 管理

```bash
# 現在の secret 一覧
npx wrangler secret list

# secret の設定 / 更新（値は標準入力から渡す）
echo -n "値" | npx wrangler secret put 変数名
```

### 必須 secret（値は手元の `.env` を参照）

| Secret | 用途 |
|---|---|
| `OPENAI_API_KEY` | LLM 呼び出し |
| `NOTION_OAUTH_ACCESS_TOKEN` | Notion API |
| `SLACK_BOT_TOKEN` | Slack API。**`users:read` スコープ必須**（担当者名の解決に使用） |
| `SLACK_SIGNING_SECRET` | Slack リクエスト署名検証 |
| `SLACK_PMO_CHANNEL_ID` | PMO 通知先チャンネル |
| `SLACK_PM_USER_ID` | PM ユーザー |
| `SLACK_WEBHOOK_URL` | エラー/通知 webhook |
| `SPRINT_DB_URL` | スプリント DB |
| `TASK_DB_URL` | タスク DB |
| `PROJECT_DB_ID` | プロジェクト DB |
| `MEMBER_SLACK_MAP` | メンバー名→Slack ID（担当者解決の起点） |
| `MEMBER_WHITELIST` | 朝の通知対象メンバー |
| `GOOGLE_SHEETS_API_KEY` / `GOOGLE_SHEETS_ID` / `GOOGLE_SHEETS_RANGE` | スケジュール連携 |

### `.env` から一括投入する例

```bash
bash -c '
set -a && source .env && set +a
for var in OPENAI_API_KEY NOTION_OAUTH_ACCESS_TOKEN SLACK_BOT_TOKEN SLACK_SIGNING_SECRET \
           SLACK_PMO_CHANNEL_ID SLACK_PM_USER_ID SLACK_WEBHOOK_URL SPRINT_DB_URL TASK_DB_URL \
           PROJECT_DB_ID MEMBER_SLACK_MAP MEMBER_WHITELIST GOOGLE_SHEETS_API_KEY \
           GOOGLE_SHEETS_ID GOOGLE_SHEETS_RANGE; do
  val="${!var}"
  [ -z "$val" ] && { echo "SKIP $var (empty)"; continue; }
  printf "%s" "$val" | npx wrangler secret put "$var" --name notion-sprint-worker-dev
done
'
```

> `vars`（`OPENAI_MODEL`, `NOTIFY_PROPERTIES`, `TEAM_FILTER`, `DRY_RUN`, `MEMBER_EXCLUDE`,
> `MEMBER_EXTRA`）は `wrangler.toml` に平文で記載済みなので secret 不要。

## Slack App 設定（変更時のみ）

Slack App 管理画面（https://api.slack.com/apps → ALOHA_CAT）:

- **Event Subscriptions** → Request URL:
  `https://notion-sprint-worker-dev.tomoya-kotetsu.workers.dev/slack/events`
- **Interactivity & Shortcuts** → Request URL:
  `https://notion-sprint-worker-dev.tomoya-kotetsu.workers.dev/slack/interactions`
- **OAuth & Permissions** → Bot Token Scopes に `users:read` が必要

## ロールバック

```bash
# 過去バージョン一覧
npx wrangler deployments list

# 特定バージョンに戻す
npx wrangler rollback [VERSION_ID]
```

## 注意事項

- **DRY_RUN**: `wrangler.toml` で `false`（本番反映ON）。動作確認だけしたい時は一時的に `true`。
- **cron 上限**: Cloudflare Free プランは「展開後」5 個まで。
  `"10,20,30,40,50 0 * * *"` は 5 トリガー換算。現在は `"0 23 * * *"` の 1 個のみ設定。
  定期通知（担当者通知/PMレポート/リマインド）をフル稼働させたい場合は、cron を展開後
  5 個以内に収めて `wrangler.toml` に追加し直す必要がある。
- **担当者名の解決**: `SLACK_BOT_TOKEN` に `users:read` が無いと `user_not_found` になり
  担当者が設定されない。スコープ変更後は Slack App を Reinstall し、新トークンを
  `wrangler secret put SLACK_BOT_TOKEN` で更新すること。
- Slack の `users.info` / `conversations.info` は GET で呼ぶ（POST+JSON だと
  `user_not_found` / `invalid_arguments` になる）。

## トラブルシュート

| 症状 | 原因 / 対処 |
|---|---|
| deploy がエラーで止まる | 別アカウントでログイン中。`wrangler whoami` 確認 → `wrangler login` |
| Slack で反応するが変更が反映されない | デプロイ先 Worker 名を確認。本番は `notion-sprint-worker-dev` |
| 担当者が「Notion ユーザー未検出」 | `SLACK_BOT_TOKEN` の `users:read` スコープ、`MEMBER_SLACK_MAP` を確認 |
| 承認ボタンでタイムアウト表示 | 3 秒以内に ack しているか（`handleSlackInteractions` の即時 Response） |
| cron 上限エラー | 展開後 5 個を超えている。cron を減らす |


メンバー	項目
古鉄	2回ジム